export type Translation = {
  popup: {
    title: string;
    charsCount: string;
    save: string;
    interfaceLanguage: string;
    donate: string;
    saved: string;
  };

  popover: {
    charsCount: string;
    paragraphs: string;
    insert: string;
    cancel: string;
  };

  context: {
    paste: string;
  };
};
