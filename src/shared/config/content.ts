import { ContentType } from '../model/types';

export const CONTENT_TYPES: ContentType[] = [
  'text',
  'title',
  // 'email',
  // 'link',
  // 'phone',
  // 'address',
  // 'firstName',
  // 'lastName',
];
