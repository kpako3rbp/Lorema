{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "nx build extension",
    "dev": "nx dev extension",
    "typecheck": "nx typecheck extension",
    "lint": "nx lint extension",
    "lint-all": "nx run-many -t typecheck lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.0.268",
    "eslint-config-rtkit": "^3.0.5",
    "eslint-plugin-boundaries": "^6.0.2",
    "husky": "^9.1.7",
    "nx": "^23.0.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}